node_modules/
dist/
.env
.env.local
*.sqlite
*.sqlite3
.tokensave/
.DS_Store
.vercel

# Per-machine agent config. .claude/settings.json IS versioned — it is the shared
# permission allowlist; only local overrides stay out.
.claude/settings.local.json

# Scratch state written by tooling that happens to be installed on one machine.
.claude/.headroom_wrap_marker.json

# Maintainer's local working notebooks, deliberately not in the repo.
# Everything an agent needs is in AGENTS.md.
PLAN.md
STATUS.md
