---
"hevy-mcp": patch
---

Add analytics engine (e1RM, records, volume, consistency, compare) as a pure internal library. Not yet exposed as MCP tools — that lands once the read-tools branch (F3) is merged.
